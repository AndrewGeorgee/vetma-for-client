import 'package:flutter/material.dart';

class SetingScreenNav extends StatelessWidget {
  const SetingScreenNav({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Scaffold());
  }
}
